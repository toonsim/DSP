function [ simin, nbsecs, fs] = initparams( toplay, fs )
%INITPARAMS Summary of this function goes here
%   Detailed explanation goes here

fs = fs;
nbsamps = length(toplay);
simin = [zeros(2*fs,2);toplay toplay; zeros(1*fs,2)]



end

